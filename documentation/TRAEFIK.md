# Traefik

## Configuration

traefik_conf.yml file is used to setup the Traefik proxy.

- NOTICE: element already present in the conf file CANNOT be overriden with the `command:` parameters from the docker-compose.yaml file